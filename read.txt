html:*************************************************************************************
<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Amazon Clone</title>
    <link rel="stylesheet" href="./Style.css" />
    <script defer type="module" src="./js/main.js"></script>
    <script type="module" src="./js/cart.js"></script>
  </head>
  <body>
    <header class="main-header">
      <div class="logo">
        <img src="./imagenes/amazon_PNG11.png" alt="Logotipo de Amazon" />
      </div>

      <div class="location nav-hover">
        <span>Enviar a</span>
        <strong>España</strong>
      </div>

      <div class="search-container">
        <select class="search-category">
          <option>Todos</option>
        </select>
        <input type="text" placeholder="Buscar en Amazon.es" />
        <button class="search-btn">🔍</button>
      </div>

      <div class="nav-tools">
        <div class="dropdown nav-hover">
          <span class="dropbtn">🇪🇸 ES ▾</span>
          <div class="dropdown-content">
            <a href="#">Español (ES)</a>
            <a href="#">English (EN)</a>
            <a href="#">Português (PT)</a>
          </div>
        </div>

        <div class="nav-item nav-hover">
          <span>Hola, identifícate</span>
          <strong>Cuenta y listas</strong>
        </div>

        <div class="nav-item nav-hover">
          <span>Devoluciones</span>
          <strong>y Pedidos</strong>
        </div>

        <div class="cart nav-hover">
          🛒 <strong>Cesta</strong>
          <span id="cart-count" class="cart-badge">0</span>
        </div>
      </div>
    </header>

    <nav class="sub-nav">
      <ul>
        <li class="nav-hover"><strong>≡ Todo</strong></li>
        <li class="nav-hover">Amazon Haul</li>
        <li class="nav-hover">Supermercado</li>
        <li class="nav-hover">Ofertas</li>
        <li class="nav-hover">Amazon Basics</li>
        <li class="nav-hover">Informática</li>
      </ul>
    </nav>

    <main class="content-grid">
      <section class="hero-section">
        <div class="placeholder-img-large">
          <img
            src="https://m.media-amazon.com/images/I/A1ACfoI4+AL._SX3000_.jpg"
            alt="Banner principal"
            class="hero-img"
          />
        </div>
      </section>

      <div class="card">
        <h3>Oferta Top</h3>
        <div class="placeholder-img">Producto</div>
        <a href="#">Ver más</a>
      </div>

      <div class="card">
        <h3>Dispositivos Amazon</h3>
        <div class="placeholder-img">Producto</div>
        <a href="#">Ver más</a>
      </div>

      <div class="card">
        <h3>Informática y accesorios</h3>
        <div class="placeholder-img">Producto</div>
        <a href="#">Ver más</a>
      </div>

      <div class="card auth-card">
        <h3>Identifícate para una mejor experiencia</h3>
        <button class="btn-primary">Inicia sesión de manera segura</button>
      </div>
    </main>

    <footer class="footer">
      <div class="footer-top">
        <a href="#">Volver arriba</a>
      </div>

      <div class="footer-links">
        <div class="column">
          <h4>Conócenos</h4>
          <a href="#">Trabajar en Amazon</a>
          <a href="#">Sobre Amazon.es</a>
          <a href="#">Sostenibilidad</a>
          <a href="#">Amazon Science</a>
        </div>

        <div class="column">
          <h4>Gana dinero con nosotros</h4>
          <a href="#">Vender en Amazon</a>
          <a href="#">Programa de afiliados</a>
          <a href="#">Logística de Amazon</a>
        </div>

        <div class="column">
          <h4>Métodos de pago Amazon</h4>
          <a href="#">Métodos de pago</a>
          <a href="#">Tarjetas regalo</a>
          <a href="#">Recarga online</a>
        </div>

        <div class="column">
          <h4>¿Necesitas ayuda?</h4>
          <a href="#">Localizar o gestionar compras</a>
          <a href="#">Tarifas y políticas de envío</a>
          <a href="#">Atención al cliente</a>
        </div>
      </div>

      <div class="footer-legal">
        <p>© 1996-2026, Amazon Clone, Inc. o sus afiliados</p>
      </div>
    </footer>
  </body>
</html>

css*********************************************************************************************************************************

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Arial, sans-serif;
}

body {
    background-color: #e3e6e6;
}

/* --- HEADER --- */
.main-header {
    background-color: #131921;
    color: white;
    display: flex;
    align-items: center;
    padding: 10px 20px;
    gap: 20px;
}

.logo {
    display: flex;
    align-items: center;
    height: 40px;        
    min-width: 100px;    
    padding: 5px;
    border: 1px solid transparent;
}

.logo img {
    max-height: 100%; 
    max-width: 100%;     
    object-fit: contain; 
}

/* Efecto hover general de la barra de navegación (borde blanco típico de Amazon) */
.nav-hover {
    padding: 8px;
    border: 1px solid transparent;
    cursor: pointer;
    border-radius: 2px;
}

.nav-hover:hover {
    border: 1px solid white;
}

/* --- BUSCADOR --- */
.search-container {
    display: flex;
    flex-grow: 1;
    height: 40px;
    border-radius: 4px;
    overflow: hidden; /* Mantiene los bordes redondeados limpios */
}

.search-container:focus-within {
    box-shadow: 0 0 0 2px #f90; /* Anillo naranja de Amazon al hacer clic */
}

.search-category {
    background: #f3f3f3;
    border: none;
    padding: 0 10px;
    border-right: 1px solid #cdcdcd;
    cursor: pointer;
}

.search-container input {
    flex-grow: 1;
    border: none;
    padding: 0 10px;
    outline: none; /* Quitamos el outline por defecto porque usamos el box-shadow en el contenedor */
}

.search-btn {
    background-color: #febd69;
    border: none;
    padding: 0 20px;
    cursor: pointer;
    font-size: 16px;
}

.search-btn:hover {
    background-color: #f3a847;
}

/* --- HERRAMIENTAS DE NAVEGACIÓN --- */
.nav-tools {
    display: flex;
    align-items: center;
    gap: 10px;
}

.nav-item span {
    display: block;
    font-size: 12px;
    color: #ccc;
}

/* Dropdown */
.dropdown {
    position: relative;
    display: inline-block;
}

.dropdown-content {
    display: none;
    position: absolute;
    background-color: white;
    min-width: 150px;
    box-shadow: 0px 8px 16px rgba(0,0,0,0.2);
    z-index: 10; /* Asegura que se vea por encima de las tarjetas */
    border-radius: 3px;
    top: 100%;
    left: 0;
}

.dropdown-content a {
    color: black;
    padding: 12px 16px;
    text-decoration: none;
    display: block;
    font-size: 14px;
}

.dropdown-content a:hover {
    background-color: #f1f1f1;
    text-decoration: underline;
}

.dropdown:hover .dropdown-content {
    display: block;
}

/* --- SUB-NAVEGACIÓN --- */
.sub-nav {
    background-color: #232f3e;
    color: white;
    padding: 5px 20px;
}

.sub-nav ul {
    display: flex;
    list-style: none;
    gap: 5px;
    font-size: 14px;
    align-items: center;
}

/* --- MAIN CONTENT & GRID --- */
.content-grid {
    display: grid;
    /* ESTA ES LA CLAVE RESPONSIVA: auto-fit adapta las columnas según el tamaño de la pantalla */
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 20px;
    padding: 20px;
    max-width: 1500px;
    margin: 0 auto;
    /* Un pequeño truco para que las tarjetas suban por encima del banner como en el diseño real */
    margin-top: -150px; 
    z-index: 2;
    position: relative;
}

/* --- HERO BANNER --- */
.hero-section {
    grid-column: 1 / -1; /* Ocupa todo el ancho siempre */
    z-index: 1;
}

.placeholder-img-large {
    width: 100%;
    height: 400px;
    display: flex;
    align-items: flex-start; /* Alinea arriba para que se vea bien si se corta */
    justify-content: center;
    overflow: hidden;
    background: linear-gradient(to bottom, #ff5722, #e3e6e6); /* Degradado sutil hacia el color de fondo */
}

.hero-img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* Evita que la imagen se deforme */
    object-position: top;
}

/* --- TARJETAS --- */
.card {
    background: white;
    padding: 20px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    z-index: 5;
}

.card h3 {
    font-size: 21px;
    margin-bottom: 10px;
}

.card a {
    color: #007185;
    text-decoration: none;
    font-size: 13px;
    margin-top: 15px;
}

.card a:hover {
    color: #c45500;
    text-decoration: underline;
}

.placeholder-img {
    background: #f8f8f8;
    height: 280px;
    margin: 15px 0;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #888;
}

.auth-card {
    text-align: center;
}

.btn-primary {
    background-color: #ffd814;
    border: 1px solid #fcd200;
    border-radius: 8px;
    padding: 10px;
    cursor: pointer;
    margin-top: 15px;
    font-weight: bold;
    transition: background-color 0.2s;
}

.btn-primary:hover {
    background-color: #f7ca00;
}

/* --- FOOTER (Añadido para que coincida con tu HTML) --- */
.footer {
    background-color: #232f3e;
    color: white;
    margin-top: 40px;
}

.footer-top {
    background-color: #37475a;
    text-align: center;
    padding: 15px;
}

.footer-top a {
    color: white;
    text-decoration: none;
    font-size: 13px;
}

.footer-top:hover {
    background-color: #485769;
}

.footer-links {
    display: flex;
    justify-content: center;
    flex-wrap: wrap; /* Para que en móviles caigan en columnas */
    gap: 80px;
    padding: 40px 20px;
    max-width: 1000px;
    margin: 0 auto;
}

.footer-links .column {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.footer-links h4 {
    font-size: 16px;
    margin-bottom: 5px;
}

.footer-links a {
    color: #DDDDDD;
    text-decoration: none;
    font-size: 14px;
}

.footer-links a:hover {
    text-decoration: underline;
}

.footer-legal {
    background-color: #131a22;
    text-align: center;
    padding: 30px 20px;
    font-size: 12px;
    color: #ddd;
}



* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Arial, sans-serif;
}

body {
    background-color: #e3e6e6;
}


.main-header {
    background-color: #131921;
    color: white;
    display: flex;
    align-items: center;
    padding: 10px 20px;
    gap: 20px;
}

.logo {
    font-weight: bold;
    font-size: 20px;
    padding: 10px;
    border: 1px solid transparent;
}

.search-container {
    display: flex;
    flex-grow: 1;
    height: 40px;
}

.search-category {
    background: #f3f3f3;
    border-radius: 4px 0 0 4px;
    border: none;
    padding: 0 10px;
}

.search-container input {
    flex-grow: 1;
    border: none;
    padding: 0 10px;
}

.search-btn {
    background-color: #febd69;
    border: none;
    padding: 0 15px;
    border-radius: 0 4px 4px 0;
    cursor: pointer;
}


.nav-tools {
    display: flex;
    align-items: center;
    gap: 20px;
}

.nav-item span {
    display: block;
    font-size: 12px;
}

.dropdown {
    position: relative;
    display: inline-block;
    cursor: pointer;
}

.dropdown-content {
    display: none;
    position: absolute;
    background-color: white;
    min-width: 150px;
    box-shadow: 0px 8px 16px rgba(0,0,0,0.2);
    z-index: 1;
    border-radius: 3px;
    top: 100%;
}

.dropdown-content a {
    color: black;
    padding: 12px 16px;
    text-decoration: none;
    display: block;
    font-size: 14px;
}

.dropdown-content a:hover {
    background-color: #f1f1f1;
    text-decoration: underline;
}

.dropdown:hover .dropdown-content {
    display: block;
}

.logo {
    display: flex;
    align-items: center;
    height: 40px;        
    min-width: 100px;    
    padding: 5px;
}

.logo img {
    max-height: 100%; 
    max-width: 100%;     
    width: auto;         
    height: auto;        
    object-fit: contain; 
}


.sub-nav {
    background-color: #232f3e;
    color: white;
    padding: 8px 20px;
}

.sub-nav ul {
    display: flex;
    list-style: none;
    gap: 15px;
    font-size: 14px;
}

.content-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    padding: 20px;
    max-width: 1500px;
    margin: 0 auto;
}


.hero-banner {
    grid-column: span 4;
    background: #fff;
    
    text-align: center;
}

.placeholder-img-large {
    width: 100%;
    height: 500px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    

    background: #ff5722;
    
    
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
}

.hero-banner img {
    width: 100%;
    height: auto;
    display: block;
}


.card {
    background: white;
    padding: 20px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
}

.placeholder-img {
    background: #eee;
    height: 200px;
    margin: 15px 0;
    display: flex;
    align-items: center;
    justify-content: center;
}

.auth-card {
    text-align: center;
}

.btn-primary {
    background-color: #ffd814;
    border: 1px solid #fcd200;
    border-radius: 8px;
    padding: 10px;
    cursor: pointer;
    margin-top: 10px;
}


/* =========================================
   RESETEO BÁSICO
========================================= */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Arial, sans-serif;
}

body {
    background-color: #e3e6e6;
}

/* =========================================
   HEADER PRINCIPAL
========================================= */
.main-header {
    background-color: #131921;
    color: white;
    display: flex;
    align-items: center;
    padding: 10px 20px;
    gap: 20px;
}

.logo {
    display: flex;
    align-items: center;
    height: 40px;        
    min-width: 100px;    
    padding: 5px;
    border: 1px solid transparent;
}

.logo img {
    max-height: 100%; 
    max-width: 100%;     
    width: auto;         
    height: auto;        
    object-fit: contain; 
}

/* Efecto hover general de la barra de navegación */
.nav-hover {
    padding: 8px;
    border: 1px solid transparent;
    cursor: pointer;
    border-radius: 2px;
}

.nav-hover:hover {
    border: 1px solid white;
}

/* =========================================
   BARRA DE BÚSQUEDA
========================================= */
.search-container {
    display: flex;
    flex-grow: 1;
    height: 40px;
    border-radius: 4px;
    overflow: hidden;
}

.search-container:focus-within {
    box-shadow: 0 0 0 2px #f90; 
}

.search-category {
    background: #f3f3f3;
    border: none;
    padding: 0 10px;
    border-right: 1px solid #cdcdcd;
    border-radius: 4px 0 0 4px; /* Detalle de tu CSS importante */
    cursor: pointer;
}

.search-container input {
    flex-grow: 1;
    border: none;
    padding: 0 10px;
    outline: none; 
}

.search-btn {
    background-color: #febd69;
    border: none;
    padding: 0 20px;
    border-radius: 0 4px 4px 0; /* Detalle de tu CSS importante */
    cursor: pointer;
    font-size: 16px;
}

.search-btn:hover {
    background-color: #f3a847;
}

/* =========================================
   HERRAMIENTAS Y DROPDOWN
========================================= */
.nav-tools {
    display: flex;
    align-items: center;
    gap: 10px;
}

.nav-item span {
    display: block;
    font-size: 12px;
    color: #ccc;
}

.dropdown {
    position: relative;
    display: inline-block;
    cursor: pointer;
}

.dropdown-content {
    display: none;
    position: absolute;
    background-color: white;
    min-width: 150px;
    box-shadow: 0px 8px 16px rgba(0,0,0,0.2);
    z-index: 10; 
    border-radius: 3px;
    top: 100%;
    left: 0;
}

.dropdown-content a {
    color: black;
    padding: 12px 16px;
    text-decoration: none;
    display: block;
    font-size: 14px;
}

.dropdown-content a:hover {
    background-color: #f1f1f1;
    text-decoration: underline;
}

.dropdown:hover .dropdown-content {
    display: block;
}

/* =========================================
   MENÚ SECUNDARIO (SUB-NAV)
========================================= */
.sub-nav {
    background-color: #232f3e;
    color: white;
    padding: 5px 20px;
}

.sub-nav ul {
    display: flex;
    list-style: none;
    gap: 15px;
    font-size: 14px;
    align-items: center;
}

/* =========================================
   GRID DE CONTENIDO Y BANNER
========================================= */
.content-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    padding: 20px;
    max-width: 1500px;
    margin: 0 auto;
}

.hero-section, .hero-banner {
    grid-column: 1 / -1; 
    z-index: 1;
    text-align: center;
}

.placeholder-img-large {
    width: 100%;
    height: 400px;
    display: flex;
    align-items: flex-start; 
    justify-content: center;
    overflow: hidden;
    background: linear-gradient(to bottom, #ff5722, #e3e6e6); 
    color: white;
}

.hero-img, .hero-banner img {
    width: 100%;
    height: 100%;
    object-fit: cover; 
    object-position: top;
    display: block;
}

/* =========================================
   TARJETAS
========================================= */
.card {
    background: white;
    padding: 20px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    z-index: 5;
}

.card h3 {
    font-size: 21px;
    margin-bottom: 10px;
}

.card a {
    color: #007185;
    text-decoration: none;
    font-size: 13px;
    margin-top: 15px;
}

.card a:hover {
    color: #c45500;
    text-decoration: underline;
}

.placeholder-img {
    background: #f8f8f8;
    height: 280px;
    margin: 15px 0;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #888;
}

.auth-card {
    text-align: center;
}

.btn-primary {
    background-color: #ffd814;
    border: 1px solid #fcd200;
    border-radius: 8px;
    padding: 10px;
    cursor: pointer;
    margin-top: 15px;
    font-weight: bold;
    transition: background-color 0.2s;
}

.btn-primary:hover {
    background-color: #f7ca00;
}

/* =========================================
   FOOTER
========================================= */
.footer {
    background-color: #232f3e;
    color: white;
    margin-top: 40px;
}

.footer-top {
    background-color: #37475a;
    text-align: center;
    padding: 15px;
}

.footer-top a {
    color: white;
    text-decoration: none;
    font-size: 13px;
}

.footer-top:hover {
    background-color: #485769;
}

.footer-links {
    display: flex;
    justify-content: center;
    flex-wrap: wrap; 
    gap: 80px;
    padding: 40px 20px;
    max-width: 1000px;
    margin: 0 auto;
}

.footer-links .column {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.footer-links h4 {
    font-size: 16px;
    margin-bottom: 5px;
}

.footer-links a {
    color: #DDDDDD;
    text-decoration: none;
    font-size: 14px;
}

.footer-links a:hover {
    text-decoration: underline;
}

.footer-legal {
    background-color: #131a22;
    text-align: center;
    padding: 30px 20px;
    font-size: 12px;
    color: #ddd;
}
*******************************************************************************************************
js.cart:
let cart = [];

// Esta es la única función que exportamos para main.js
// Ahora recibe el "game" completo, ¡ya no necesita buscar en localdata!
export function addToCartLogic(card) {
  
  // 1. Buscamos si ya está en la cesta (¡Ojo! es cart.find, no card.find)
  const existingItem = cart.find((item) => item.id === card.id);
  const currentQty = existingItem ? existingItem.quantity : 0;

  // 2. Validar stock (Usamos game.stock)
  if (currentQty + 1 > card.stock) {
    alert("No hay suficiente stock disponible para este producto.");
    return;
  }

  // 3. Añadir o sumar
  if (existingItem) {
    existingItem.quantity++;
  } else {
    cart.push({
      id: card.id,
      title: card.title,
      price: card.price, // Si falla el precio, asegúrate de que tu JSON tiene "price"
      quantity: 1,
    });
  }
  
  saveCart(); 
  updateCart();
}

function removeFromCart(id) {
  cart = cart.filter((item) => item.id !== id);
  saveCart();
  updateCart();
}

function saveCart() {
  localStorage.setItem("shoppingCart", JSON.stringify(cart));
}

function updateCart() {
    const badge = document.getElementById("cart-count");
    if (badge) {
        // En lugar de cart.length (que cuenta juegos únicos),
        // reduce() sumará la cantidad total de artículos. ¡Pruébalo!
        badge.textContent = cart.reduce((total, item) => total + item.quantity, 0);
    }
}

function loadCart() {
  const savedCart = localStorage.getItem("shoppingCart");
  if (savedCart) {
    try {
      cart = JSON.parse(savedCart);
      updateCart();
    } catch (e) {
      console.error("Error parseando el carrito", e);
      cart = [];
    }
  }
}

// Cargamos el carrito al iniciar este archivo
loadCart();
**************************
main.js
// 1. Referencia a los elementos del DOM corregida
const contentGrid = document.querySelector(".content-grid");
import { addToCartLogic } from './cart.js';
//Aqui hago publica la funcion.

window.addCart = function(id) {
    const cardSelected = localdata.find((g) => g.id === id);
    if (cardSelected) {
        addToCartLogic(cardSelected ); // Le pasamos el objeto entero
    }
};
let localdata = [];
 
// Tu función init y fetch van aquí...
console.log(contentGrid);

// Variables globales


//---------------------------------------------//
// Funciones de datos
//---------------------------------------------//

async function getData() {
  try {
    const response = await fetch("./data/main-data.json");
    if (!response.ok) {
      throw new Error("Error cargando los datos");
    }
    return await response.json();
  } catch (error) {
    console.error("Error:", error);
    if(contentGrid) {
        contentGrid.innerHTML = "<p>Error cargando los videojuegos.</p>";
    }
  }
}

//---------------------------------------------//
// Función Render Principal
//---------------------------------------------//

function renderCards(data) {
  // Limpiamos e inyectamos la sección del Hero primero
  contentGrid.innerHTML = `
    <section class="hero-section">
        <div class="placeholder-img-large">
            <img src="https://m.media-amazon.com/images/I/A1ACfoI4+AL._SX3000_.jpg" alt="Banner principal" class="hero-img">
        </div>
    </section>
  `;

  // Iteramos sobre los datos
  data.forEach((item) => {
    const div = document.createElement("div");
    div.className = "card"; 
    div.innerHTML = `
        <h3>${item.title}</h3>
        <div class="placeholder-img">
            <img src="${item.image}" alt="${item.title}">
        </div>
        <a href="#">Ver más</a>
        <button class="add-cart-btn" onclick="addCart(${item.id})">Añadir al carrito</button>
    `;
    contentGrid.appendChild(div); 
  });
}


//---------------------------------------------//
// Funciones de LocalStorage
//---------------------------------------------//

// Inicializar
async function init() {
  const data = await getData();
  if (data) { 
    localdata = data
    renderCards(data);
  }
}

init();

